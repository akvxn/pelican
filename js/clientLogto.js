import { LogtoClient } from '@logto/browser';

export const logtoClient = new LogtoClient({
  endpoint: 'https://b5q39c.logto.app/',
  appId: 'jg81pom9gvg9fgoj94uln',
  redirectUri: 'https://pélican.fr/callback', // adapte ici ton domaine
});
